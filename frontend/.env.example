# Dev navigateur (sans Tauri) : URL et token du moteur IA.
# Dans l'app Tauri, l'endpoint est fourni par la commande IPC `get_engine_endpoint`.
VITE_ENGINE_URL=http://127.0.0.1:8765
VITE_ENGINE_TOKEN=dev-token
